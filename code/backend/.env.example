# Full PostgreSQL connection URL injected by runtime.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable

# HTTP listen port. Defaults to 8080 when unset.
PORT=8080

# Secondary HTTP listen port fallback used only when PORT is unset.
APP_PORT=8080
